---
type: 配置文件
category: 课程模块
belongsTo: ai-apprentice-guide（AI 启蒙导师专家包）
module: 07
version: 0.1.0
---

> 本文件是配置文件（课程模块），属于 ai-apprentice-guide 专家包。

# 模块 07：迭代（用提示词工程的方法持续精进）

> 目的：把"输出不满意"从放弃/重开，变成一套可复制的**迭代方法**——通过改进提示词，让 AI 的产出一次比一次好。

## 教学目标

学习者能独立完成"输出 → 审核 → 反馈 → 再生成"的迭代循环，并用迭代方法打磨出高质量产出。

## 内容要点

1. **迭代循环（核心）**：
   > 输出 → 审核（模块 06 方法）→ 反馈（改进提示词）→ 重新生成 → 再审核
2. **反馈式改进提示词（承接模块 02）**：
   - 指出哪里不对：具体到"第 2 点太笼统""语气太正式"
   - 补充要求：加约束、加例子、限定格式
   - 给正例：把满意的部分指给它，让它照着来
3. **迭代记录**：每次改了什么、效果如何——保留痕迹，模板如下：
   ```
   ## 第 N 轮迭代
   - 问题：……
   - 改动：……（改了提示词的哪部分）
   - 效果：……
   ```
4. **与实战结合**：把迭代方法用在模块 05 的实战项目上，打磨交付物。
5. **迭代心态**：好产出是"磨"出来的，不是一次生成的；大胆干，多轮磨。

## 完成标准（可验证）

- [ ] 对同一任务完成至少 3 轮迭代，每轮有迭代记录（问题/改动/效果）
- [ ] 最终产出比第一轮明显更好，能说出"哪几个改动起了关键作用"

## 总纲回顾

"AI 的上限，是会用的人的想象。"
