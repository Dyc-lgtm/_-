---
type: 配置文件
category: 课程模块
belongsTo: ai-apprentice-guide（AI 启蒙导师专家包）
module: 03
version: 0.1.0
---

> 本文件是配置文件（课程模块），属于 ai-apprentice-guide 专家包。

# 模块 03：架构认知（拆黑盒 · 重点模块）

> 目的：让学习者真正摸到架构——不是背名词，而是知道"一个 AI 系统/Agent 由哪些大的部分组成、它们之间什么关系、数据怎么流动、文件怎么组织的"。
> 本模块是课程的地基，数据流是重中之重。

## 教学目标

学习者能画出/讲清一个 AI 应用系统的完整组成与数据流路径，理解"数据打通"的含义。

## 内容要点

### 1. AI 系统的大组成（完整拆解）

| 组成 | 是什么 | 生活化理解（括号） |
|---|---|---|
| 模型端 | 大模型本体（如 GPT/Claude/DeepSeek），决定"智力上限" | 一个非常博学但偶尔自信胡说的顾问 |
| 输入与上下文端 | 用户输入、系统提示词、上下文窗口（对话历史） | 你递给顾问的资料夹，夹子有大小限制 |
| 编排层 | Agent 框架：Markdown 编排、工作流、任务编排、规范 | 顾问的"工作手册"：先干什么、按什么规矩来 |
| 工具层 | 工具调用、连接器（MCP）、外部 API | 顾问能打电话、查库房、开电脑的能力 |
| 数据层 | 知识库、记忆、文件、数据库、外部数据源 | 顾问的"长期笔记本"和"仓库钥匙" |
| 执行与自动化 | 定时任务、自动化流程、批量处理 | 不用你催，顾问到点自己干活 |
| 治理层 | 权限、密钥、审计、边界控制 | 保险柜：谁能碰什么，都记下来 |
| 交付端 | PC、手机/小程序等多端形态 | 顾问的"办公室"：你在哪都能找到它 |

**关键：这 8 大块不是孤立的，是连起来的——连接靠的就是数据流。**

### 2. Agent 组成拆解（分开讲，再讲关系）

- **Agent = Markdown 编排 + 底层逻辑框架**。
- 分开讲：
  - Markdown 编排（人给 Agent 写的：身份、规范、流程、边界——就是"配置文件"里的那些 md 文件）
  - 底层逻辑框架（平台/框架提供的能力：模型调用、工具调用、任务执行、记忆存取）
- 再讲关系：**编排定义"做什么、怎么做"，框架提供"能做到的能力"**。同一个框架，换一套编排，就是完全不同的 Agent。就像同一台电脑（框架），装不同的软件（编排），能做的事就完全不一样。

### 3. 文件之间的关系（配置是怎么组织起来的）

- 一个专家包/Agent 通常由多个文件组成：清单文件（plugin.json）、本体文件（agents/*.md）、技能文件（skills/*/SKILL.md）、素材文件（references/、avatars/）等。
- 关系：**清单文件声明"有哪些文件"→ 本体文件定义"我是谁"→ 技能文件提供"我会什么"→ 素材文件支撑"我讲什么"**。
- 改一个文件的影响：改本体 = 换人格；改技能 = 换能力；改素材 = 换内容。**知道文件之间的关系，才知道改哪里、会不会改坏。**

### 4. 数据流（本模块重中之重：数据打通）

完整数据流路径：

> 用户输入 → 编排层组装上下文（提示词 + 记忆 + 知识库）→ 模型推理 → 输出 →（需要时）工具调用 → 数据回写 → 记忆更新

讲清每一跳：
- 用户输入从哪进（对话框/小程序端）
- 上下文怎么组装（系统提示词 + 历史对话 + 检索到的资料——这就是"数据打通"：把分散的数据汇到模型面前）
- 模型怎么推理（基于上下文"接话"，不是查资料——为幻觉埋线）
- 输出怎么回（回给用户 / 触发工具）
- 工具调用的数据怎么回流（查了天气 → 数据回来 → 再组织成回答）
- 记忆怎么更新（把新信息写回长期记忆，下次能用）

**"数据打通"的意思**：数据在 输入→上下文→模型→工具→记忆 之间能顺畅流动。**不通的地方就是 Agent 干不了活的地方**。判断一个 Agent 行不行，看数据通不通。

### 5. 幻觉机制（自然带出）

- 模型是"接话"不是"查资料"，接得自信不代表接得对。
- 幻觉 = 自信的编造。预防靠"数据打通"（给它真实资料）+ 模块 06 的审核方法。

### 6. Token（自然带一句，不展��）

- Token 是模型处理文本的基本单位，也是一种算力资源（好比燃料），决定成本与单次处理的量。它总会了解的，这里知道"有这回事"即可。

## 完成标准（可验证）

- [ ] 能画出/讲清"用户输入 → 上下文 → 模型 → 输出 → 工具 → 记忆"的完整数据流路径
- [ ] 能说出 AI 系统 8 大组成中至少 5 个，并讲清它们之间的 2 组关系
- [ ] 能讲清"Agent = Markdown 编排 + 底层逻辑框架"是什么意思

## 总纲回顾

"知道数据怎么流，就知道它在干什么。"
